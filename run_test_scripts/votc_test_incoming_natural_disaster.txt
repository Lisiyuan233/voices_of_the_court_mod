# VOTC incoming letter test: natural disaster report.
# Console command: run votc_test_incoming_natural_disaster.txt
# Copy to: C:\Users\A\Documents\Paradox Interactive\Crusader Kings III\run

save_scope_as = actor

random_vassal = {
	limit = {
		votc_incoming_letter_disaster_reporter_valid_trigger = yes
	}
	save_scope_as = recipient
}

if = {
	limit = { exists = scope:recipient }
	set_variable = { name = votc_disaster_type value = flag:earthquake }
	votc_start_incoming_letter_effect = { TYPE = flag:natural_disaster }
}
else = {
	debug_log = "VOTC:INCOMING_LETTER/;/test/;/skipped/;/no_reporter"
}
